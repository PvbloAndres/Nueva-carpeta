import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.duocuc.ponate',
  appName: 'ponate',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
